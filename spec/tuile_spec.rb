# frozen_string_literal: true

RSpec.describe Tuile do
  it "has a version number" do
    expect(Tuile::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
